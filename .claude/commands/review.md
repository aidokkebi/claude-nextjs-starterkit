방금 작성하거나 수정한 코드를 리뷰해주세요. 아래 순서로 진행합니다:

1. `git diff HEAD` 와 `git status` 로 변경된 파일과 내용을 파악합니다.
2. 변경된 코드를 아래 기준으로 리뷰합니다:
   - **정확성**: 로직 오류, 엣지 케이스 누락, 버그 가능성
   - **보안**: XSS, SQL 인젝션, 민감 정보 노출 등 OWASP Top 10 취약점
   - **성능**: 불필요한 렌더링, 비효율적인 쿼리, 메모리 누수 가능성
   - **가독성**: 함수/변수명, 코드 구조, 불필요한 복잡도
   - **타입 안전성**: TypeScript 타입 오류 또는 `any` 남용
3. 문제가 있는 경우 파일 경로와 줄 번호를 명시하고 개선 방법을 제안합니다.
4. 전반적인 평가를 한 줄로 요약합니다.

리뷰 결과는 한국어로 작성합니다.
